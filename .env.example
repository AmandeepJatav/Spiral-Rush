# Spiral Rush — environment configuration (copy to .env)
PORT=3000
# CORS origin for Socket.io (set to your domain in production, e.g. https://play.example.com)
CORS_ORIGIN=*
# logging: LOG_LEVEL=debug|info|warn|error ; LOG_JSON=1 for JSON lines (log aggregators)
LOG_LEVEL=info
LOG_JSON=0
